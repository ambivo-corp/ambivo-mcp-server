{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "sh -c 'ambivo-gpt-actions serve --host 0.0.0.0 --port ${PORT:-8080}'"
  }
}