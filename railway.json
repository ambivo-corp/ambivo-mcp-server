{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "python -m ambivo_gpt_actions.test_app --host 0.0.0.0 --port ${PORT:-8080}"
  }
}